# Copy to .env.secrets and fill in real values (do not commit).

# NordVPN or provider credentials (OpenVPN)
OPENVPN_USER=
OPENVPN_PASSWORD=

# WireGuard credentials (preferred with Nord if supported)
WIREGUARD_PRIVATE_KEY=
WIREGUARD_ADDRESSES=10.5.0.2/32

# Optional: restrict server selection
# SERVER_HOSTNAMES=
# SERVER_CITIES=
# SERVER_COUNTRIES=

# App secrets
PLEX_CLAIM=
